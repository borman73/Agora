<div class="container-fluid centered bg-img">
  <div class="card shadow-sm p-3 pb-0" style="width: 24rem;">
    <div class="card-body">
      <h5 class="card-title text-center mb-3">My groups</h5>
        <% @groups.each do |g| %>
          <div class="mb-2">
            <%= link_to "#{g.title}", group_path(g) %>   
          </div>
        <% end %>
    </div>
  </div>
<div>